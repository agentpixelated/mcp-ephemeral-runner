name: CU review pages
on:
  push:
    branches: [main]
    paths:
      - 'review/cu-review.pdf'
      - 'review/images/**'
      - '.github/workflows/cu-review-pages.yml'
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Prepare static review site
        run: |
          mkdir -p _site/review
          cp review/cu-review.pdf _site/review/cu-review.pdf
          cp -r review/images _site/review/images
          cat > _site/index.html <<'EOF'
          <!doctype html><html><body><h1>C:U temporary visual review</h1><p><a href="review/cu-review.pdf">Open review PDF</a></p><p><a href="review/images/overview/nav_00.jpg">Overview image</a></p></body></html>
          EOF
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
