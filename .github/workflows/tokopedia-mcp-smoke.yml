name: Tokopedia MCP smoke

on:
  push:
    paths:
      - 'agent-runtime/dispatch/tokopedia-*.json'

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Resolve dispatch file
        id: dispatch
        shell: bash
        run: |
          set -euo pipefail
          file="$(git diff-tree --no-commit-id --name-only -r "$GITHUB_SHA" | grep '^agent-runtime/dispatch/tokopedia-.*\.json$' | tail -n 1)"
          test -n "$file"
          echo "file=$file" >> "$GITHUB_OUTPUT"
          echo "id=$(basename "$file" .json)" >> "$GITHUB_OUTPUT"
      - name: Run Tokopedia MCP job and capture logs
        shell: bash
        run: |
          set +e
          mkdir -p tokopedia-results
          node agent-runtime/run-job-file.mjs "${{ steps.dispatch.outputs.file }}" > tokopedia-results/runner.log 2>&1
          code=$?
          echo "$code" > tokopedia-results/exit-code.txt
          exit 0
      - name: Persist latest result and logs
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          src="agent-runtime/results/${{ steps.dispatch.outputs.id }}.json"
          if [[ -f "$src" ]]; then
            cp "$src" tokopedia-results/latest.json
          else
            printf '{"status":"failed","error":"result file missing; inspect runner.log"}\n' > tokopedia-results/latest.json
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tokopedia-results/latest.json tokopedia-results/runner.log tokopedia-results/exit-code.txt
          git commit -m 'chore: update Tokopedia MCP diagnostics' || exit 0
          git push
